const handleVoice = async (req, res) => {
  // Placeholder for future Voice/RAG functionality
  res.json({ message: "Voice assistant architecture initialized. Audio processing service not yet connected." });
};

module.exports = {
  handleVoice
};
